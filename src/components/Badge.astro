---
interface Props {
  class: string;
}

const { class: className } = Astro.props;
---

<div class:list={["mb-5 lg:mb-5 overflow-hidden tracking-[3px] text-xs md:px-5 md:py-2 lg:text-xs w-fit uppercase bg-white bg-opacity-5 text-[#D1AAD7] rounded-full px-4 py-2", className]}>
  <slot />
</div>